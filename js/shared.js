/* let lala = null;
let resolveLalaPromise;
const lalaPromise = new Promise((resolve) => {
    resolveLalaPromise = resolve;
});

export function setLALA(value) {
    lala = value;
    resolveLalaPromise(lala); // Resolvemos la promesa cuando el valor de lala esté disponible
}

export function getLALA() {
    return lalaPromise; // Retornamos la promesa que será resuelta cuando el valor esté listo
}
 */
